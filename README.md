# 🎵 دليل تنصيب وتشغيل بوت راديو تليجرام

## 📋 جدول المحتويات

1. [المتطلبات الأساسية](#المتطلبات-الأساسية)
2. [التنصيب خطوة بخطوة](#التنصيب-خطوة-بخطوة)
3. [الإعداد والتكوين](#الإعداد-والتكوين)
4. [تشغيل البوت](#تشغيل-البوت)
5. [طريقة الاستخدام](#طريقة-الاستخدام)
6. [حل المشاكل](#حل-المشاكل)

---

## 🔧 المتطلبات الأساسية

قبل البدء، تأكد من توفر:

### 1. نظام التشغيل
- **Linux** (Ubuntu 20.04 أو أحدث) - موصى به
- **Windows** (10/11)
- **macOS**

### 2. البرامج المطلوبة
- **Python 3.8** أو أحدث
- **FFmpeg** (لمعالجة الصوت)
- **Git** (اختياري)

### 3. حسابات تليجرام
- حساب تليجرام عادي (للحساب المساعد)
- بوت تليجرام من [@BotFather](https://t.me/BotFather)
- **API ID** و **API Hash** من [my.telegram.org](https://my.telegram.org)

---

## 📥 التنصيب خطوة بخطوة

### الخطوة 1: تثبيت Python

#### على Ubuntu/Debian:
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

#### على CentOS/RHEL:
```bash
sudo yum install python3 python3-pip
```

#### على Windows:
- نزّل Python من [python.org](https://www.python.org/downloads/)
- تأكد من تحديد خيار "Add Python to PATH"

#### على macOS:
```bash
brew install python3
```

### الخطوة 2: تثبيت FFmpeg

#### على Ubuntu/Debian:
```bash
sudo apt install ffmpeg
```

#### على CentOS/RHEL:
```bash
sudo yum install ffmpeg
```

#### على Windows:
1. نزّل FFmpeg من [ffmpeg.org](https://ffmpeg.org/download.html)
2. استخرج الملفات
3. أضف مسار مجلد `bin` إلى متغيرات البيئة

#### على macOS:
```bash
brew install ffmpeg
```

### الخطوة 3: تحميل ملفات البوت

```bash
# إنشاء مجلد للبوت
mkdir telegram_radio_bot
cd telegram_radio_bot

# إذا كنت تستخدم Git
git clone <رابط_المستودع> .

# أو نزّل الملفات يدوياً وضعها في المجلد
```

### الخطوة 4: إنشاء بيئة افتراضية (موصى به)

```bash
# إنشاء البيئة الافتراضية
python3 -m venv venv

# تفعيل البيئة الافتراضية
# على Linux/macOS:
source venv/bin/activate

# على Windows:
venv\Scripts\activate
```

### الخطوة 5: تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

إذا واجهت مشاكل، جرب:
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

---

## ⚙️ الإعداد والتكوين

### 1. الحصول على API ID و API Hash

1. افتح [my.telegram.org](https://my.telegram.org)
2. سجّل الدخول برقم هاتفك
3. اذهب إلى "API Development Tools"
4. املأ النموذج وأنشئ تطبيق جديد
5. احفظ **API ID** و **API Hash**

### 2. إنشاء البوت

1. افتح [@BotFather](https://t.me/BotFather) في تليجرام
2. أرسل `/newbot`
3. اختر اسم ومعرف للبوت
4. احفظ **Bot Token**

### 3. تعديل ملف config.py

افتح ملف `config.py` وعدّل المعلومات:

```python
# معلومات API
API_ID = 12345678  # ضع API ID هنا
API_HASH = "abc123def456"  # ضع API Hash هنا

# توكن البوت
BOT_TOKEN = "123456:ABC-DEF1234"  # ضع Bot Token هنا
```

### 4. إنشاء Session String للحساب المساعد

هذا مهم جداً! يحتاج البوت لحساب مساعد للانضمام للمكالمات الصوتية.

```bash
python generate_session.py
```

سيطلب منك:
1. **API ID** - أدخله
2. **API Hash** - أدخله
3. **رقم الهاتف** - أدخل رقمك (مع رمز الدولة: +964...)
4. **رمز التحقق** - أدخل الرمز المرسل لك في تليجرام
5. **كلمة المرور** (إذا كان لديك تحقق بخطوتين)

بعد النجاح، سيعطيك **Session String**. انسخه وضعه في `config.py`:

```python
SESSION_STRING = "النص_الطويل_هنا"
```

---

## 🚀 تشغيل البوت

### التشغيل العادي

```bash
python bot.py
```

### التشغيل في الخلفية (Linux)

```bash
# باستخدام nohup
nohup python bot.py > bot.log 2>&1 &

# أو باستخدام screen
screen -dmS radio_bot python bot.py

# عرض الجلسة
screen -r radio_bot
```

### التشغيل كخدمة (Linux Systemd)

أنشئ ملف `/etc/systemd/system/radio-bot.service`:

```ini
[Unit]
Description=Telegram Radio Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/telegram_radio_bot
Environment="PATH=/path/to/telegram_radio_bot/venv/bin"
ExecStart=/path/to/telegram_radio_bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

ثم:
```bash
sudo systemctl daemon-reload
sudo systemctl enable radio-bot
sudo systemctl start radio-bot

# للتحقق من الحالة
sudo systemctl status radio-bot

# عرض السجلات
journalctl -u radio-bot -f
```

---

## 📖 طريقة الاستخدام

### للمستخدمين العاديين

1. **ابدأ محادثة مع البوت**
   - ابحث عن البوت في تليجرام
   - أرسل `/start`

2. **أضف البوت لمجموعتك/قناتك**
   - اجعله مشرف مع صلاحيات:
     - إدارة المكالمات الصوتية
     - حذف الرسائل
     - إرسال الرسائل
   
3. **فعّل الراديو**
   ```
   /activate
   ```

4. **أضف أغاني**
   
   **من رابط:**
   ```
   /add https://youtube.com/watch?v=...
   ```
   
   **من ملف:**
   - ارفع ملف صوتي
   - رد عليه بـ `/add`

5. **ابدأ التشغيل**
   ```
   /play
   ```

### أوامر التحكم

#### 🎵 التشغيل
- `/play` - بدء التشغيل
- `/pause` - إيقاف مؤقت
- `/resume` - استئناف
- `/skip` - تخطي الأغنية
- `/stop` - إيقاف الراديو

#### 📋 إدارة القائمة
- `/add` - إضافة أغنية
- `/playlist` - عرض القائمة
- `/remove [رقم]` - حذف أغنية
- `/shuffle` - خلط القائمة

#### ⚙️ الإعدادات
- `/settings` - الإعدادات
- `/status` - الحالة الحالية
- `/autoplay` - التشغيل التلقائي

### مثال استخدام كامل

```
# 1. تفعيل الراديو
/activate

# 2. إضافة أغاني
/add https://youtube.com/watch?v=dQw4w9WgXcQ
/add https://soundcloud.com/artist/track

# 3. عرض القائمة
/playlist

# 4. بدء التشغيل
/play

# 5. خلط القائمة (اختياري)
/shuffle

# 6. تفعيل التشغيل التلقائي
/autoplay
```

---

## 🔍 حل المشاكل

### مشكلة: البوت لا يستجيب

**الحل:**
```bash
# تحقق من أن البوت يعمل
ps aux | grep bot.py

# تحقق من السجلات
tail -f bot.log

# أعد تشغيل البوت
pkill -f bot.py
python bot.py
```

### مشكلة: خطأ في التحميل من يوتيوب

**الحل:**
```bash
# حدّث yt-dlp
pip install --upgrade yt-dlp
```

### مشكلة: البوت لا ينضم للمكالمة الصوتية

**الحل:**
1. تأكد من أن الحساب المساعد مضاف للمجموعة
2. تأكد من الصلاحيات:
   ```
   /activate
   ```
3. تحقق من Session String في config.py

### مشكلة: خطأ في FFmpeg

**الحل:**
```bash
# تأكد من تثبيت FFmpeg
ffmpeg -version

# إعادة تثبيت
sudo apt install --reinstall ffmpeg
```

### مشكلة: قاعدة البيانات معطلة

**الحل:**
```bash
# احذف قاعدة البيانات وأنشئها من جديد
rm radio_bot.db
python bot.py
```

### مشكلة: استهلاك عالي للذاكرة

**الحل:**
- قلل عدد الأغاني المحفوظة
- احذف الأغاني القديمة:
  ```bash
  rm -rf downloads/*.mp3
  ```

---

## 🛠️ صيانة البوت

### تحديث المكتبات

```bash
pip install --upgrade -r requirements.txt
```

### نسخ احتياطي لقاعدة البيانات

```bash
# نسخ احتياطي
cp radio_bot.db radio_bot_backup_$(date +%Y%m%d).db

# استعادة
cp radio_bot_backup_20240101.db radio_bot.db
```

### تنظيف الملفات القديمة

```bash
# حذف الأغاني القديمة (أكثر من 30 يوم)
find downloads -type f -mtime +30 -delete
```

---

## 📊 المميزات المتقدمة

### 1. إحصائيات الاستخدام

```python
# في ملف منفصل: stats.py
from database import Database

db = Database()
stats = db.get_statistics(chat_id)

print(f"عدد الأغاني: {stats['total_songs']}")
print(f"إجمالي التشغيلات: {stats['total_plays']}")
```

### 2. جدولة التشغيل

يمكن إضافة ميزة جدولة لتشغيل الراديو في أوقات محددة:

```python
# مثال: التشغيل الساعة 8 صباحاً يومياً
import schedule
import time

def auto_start():
    asyncio.create_task(radio.start_playing(chat_id))

schedule.every().day.at("08:00").do(auto_start)
```

---

## 🔐 الأمان

### نصائح الأمان:

1. **لا تشارك Session String أبداً!**
2. احفظ `config.py` في مكان آمن
3. استخدم `.gitignore` إذا كنت تستخدم Git:
   ```
   config.py
   *.session
   *.db
   downloads/
   ```

4. فعّل التحقق بخطوتين لحساب تليجرام
5. استخدم VPS موثوق للتشغيل المستمر

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تحقق من السجلات: `tail -f bot.log`
2. راجع قسم "حل المشاكل" أعلاه
3. تأكد من تحديث جميع المكتبات
4. أعد تشغيل البوت بالكامل

---

## 📝 ملاحظات إضافية

### الأداء الأمثل:

- استخدم VPS مع 1GB RAM على الأقل
- سرعة إنترنت: 10 Mbps على الأقل
- مساحة تخزين: 20GB على الأقل

### القيود:

- الحد الأقصى لحجم الملف: 200MB (قابل للتعديل في config.py)
- الحد الأقصى للأغاني: 1000 أغنية لكل مجموعة
- التشغيل المتزامن: مجموعات غير محدودة

---

## ✅ قائمة التحقق

قبل التشغيل، تأكد من:

- [ ] تثبيت Python 3.8+
- [ ] تثبيت FFmpeg
- [ ] تثبيت جميع المكتبات من requirements.txt
- [ ] إعداد config.py بالمعلومات الصحيحة
- [ ] إنشاء Session String
- [ ] إعطاء البوت صلاحيات المشرف
- [ ] تفعيل الراديو بـ /activate

---

## 🎉 النجاح!

إذا وصلت لهنا واتبعت كل الخطوات، بوت الراديو الآن جاهز للعمل!

استمتع بتشغيل الموسيقى في مجموعاتك! 🎵

---

**تاريخ آخر تحديث:** فبراير 2026
**الإصدار:** 1.0.0
